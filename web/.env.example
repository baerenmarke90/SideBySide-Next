# S8 reference client. The pair does not enter these technical values in the UI.
# Empty API base means same-origin; Vite proxies /api to 127.0.0.1:8000 in development.
VITE_SBS_API_BASE_URL=
VITE_SBS_SPACE_ID=
